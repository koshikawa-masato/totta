# totta

書籍の見開きをライブカメラで取り込み、**LLM が読むことを前提とした PDF** を作る macOS/iOS アプリ。
所有者の目的は MS (Azure AI) Foundry 等のファイル横断ナレッジ技術の習得で、totta はそのドッグフーディング。

## コンセプト: JPEG + OCR のハイブリッド PDF

totta の PDF は「人が読むスキャン」ではなく「**LLM が読むナレッジソース**」として設計されている。
1 ページに 2 つの層があり、役割が違う:

| 層 | 内容 | 役割 |
|---|---|---|
| 画像層 | グレースケール JPEG(長辺 2400px・q0.75) | **正解**。図表・レイアウト・OCR が崩れた箇所の確認用 |
| テキスト層 | Vision OCR の透明テキスト(平均信頼度 ~0.87) | **索引**。検索・要約・横断参照用。低コスト(1 章 ≈ 1〜2 万トークン) |

### LLM(Claude)がこの PDF を読むときの原則

1. **まずテキスト層から読む**。要約・検索・構成把握はテキスト層で足りる(画像の 1/10 以下のコスト)
2. **逐語引用はテキスト層を信用しない**。OCR には誤字がある(例:「萌芽期→芽期」「収穫逓減→収穫量減」)。
   引用・数値・固有名詞の確定が必要なときは該当ページを画像として読んで原文確認する
3. **図表はテキスト層では読めない**(ラベルの羅列になる)。図の内容が必要なときだけページ画像を見る
4. テキスト量が極端に少ないページ(< 300 字/ページ)は撮影不良の可能性が高い。画像で確認し、必要なら撮り直しを提案する

### このリポジトリでのテキスト抽出方法

テキスト層は **`scripts/pdftext.py`** で取り出す(要 `pypdf` + `pdfminer.six`、この Mac に導入済み)。
コンテンツストリームの描画順(= Vision OCR の認識順)で読み、見開き 1 ページは x 位置で「左ページ / 右ページ」に分けて出す:

```bash
scripts/pdftext.py books/<書名>/<章>.pdf                 # 標準出力へ(ページ・左右の見出し付き)
scripts/pdftext.py x.pdf --pages 3-5                        # ページ指定(1 始まり)
scripts/pdftext.py books/<書名>/*.pdf --out /tmp/book_txt   # 章ごとに .txt に書き出し(Read で読む)
```

他の抽出法の注意点(2026-08-16 実測):
- **pdfminer `extract_text()`**: 文字は正しいがレイアウト解析が見開きの左右を区別せず、**左右ページの行が交互に混ざる**
- **PDFKit `page.string`**: 2026-08-16 より前の書き出しでは段落内の行順が入れ替わる(透明テキストのグリフが縦に重なるため。
  PDFExporter 側は同日修正済みで、以後の書き出しでは左→右ページ順に正しく読める)
- **pypdf 単体**: ToUnicode が無く Adobe-Japan1 CMap 未内蔵のため CID のまま文字化けする(`pdftext.py` はこれを pdfminer の CMap で補っている)
- 見開き分割(`splitSpread`、2026-08-16 から既定オン)して書き出した PDF なら、どのパーサでも左右が混ざる余地はない。`books/` の既存 PDF は分割前(見開き 1 ページ)なので上記の注意が当てはまる

ページ画像が必要なときは PDFKit `PDFPage.thumbnail(of:for:)` か `CGPDFDocument` + `CGContext.drawPDFPage` でレンダリングする
(過去セッションで `render` / `pdfsplit` 等のツールを scratchpad に作った実績あり。同じ手法で再作成できる)。
見開き 1 ページは 1152×819pt なので、2 倍程度でレンダリングし左右半分に切ると読みやすい。

## books/ — 蔵書コーパス

```
books/<著書名>_<著者名>/<著書名>_<著者名>_<章など>.pdf
例: books/技術の創造と設計_畑村洋太郎/技術の創造と設計_畑村洋太郎_第2章_失敗学のすすめ_2.1-2.2.pdf
```

- **1 ファイル 30MB 未満・100 ページ未満**に収める(claude.ai 添付制限に合わせた設計)。
  超える章は**節の切れ目で分割**し、ファイル名に節範囲(`_2.1-2.2` 等)を付ける
- 分割は再エンコードなしで行う(PDFKit でページ移送。画質劣化ゼロ・テキスト層保持)
- PDF のタイトル属性にも書名・章・節を設定する
- `books/` は **.gitignore 済み**。書籍の複製なのでリポジトリに含めない・外部共有しない(私的複製の範囲)。
  共有してよいのは本そのものではなく、そこから作った要約・スライド等の派生ナレッジのみ

### 活用パターン

- **章を渡して咀嚼**: 該当章の PDF パスを渡せば要約・構成抽出・議論ができる
- **スライド等の下調べ**: テキスト層から構造(節見出し・キーコンセプト・事例)を抽出 → 骨子を組む →
  引用が必要な箇所だけ画像で原文確認、の順で進める
- **横断参照**(蔵書が増えたら): 複数冊のテキスト層を横断検索。将来的にはローカル RAG(埋め込み検索)化を予定
- **Web 版 Claude**: 単発は直接アップロード、継続利用は Claude Projects のナレッジに登録が本命
- **Foundry(file search)等の RAG**: 索引にされるのはテキスト層だけで画像は見られない(2026-08 時点の公式仕様:
  `.pdf`/`.md` 対応、800 トークン・重なり 400 でチャンク、text-embedding-3-large、画像形式非対応)。
  そこで **PDF と、totta が書き出す .md(XY-cut で読み順整列・各ページ見出しに `<pdf名> p.N`)を同じ章単位で両方入れる**。
  引用は .md の見出しから PDF のページ画像へ辿って原文確認する。Foundry には 30MB/100 ページ制約はない(512MB / 500 万トークン)

## アプリ本体(参考)

- 構成・ビルド・使い方は `README.md` 参照。Core は `cd Core && swift test`(50+ テスト)
- 実機インストール: `scripts/install-iphone.sh`(要ロック解除)
- **データ保持ポリシー(必ず守る)**: カメラ映像は録画・保存しない。動画ファイル入力は付けない。
  フレームはメモリ内 JPEG のみ。ディスクに残るのは書き出した最終 PDF だけ。PDF 保存完了で途中データ全破棄。
  デバッグ用でも中間画像のファイル保存オプションを追加しない
- 書き出し既定値(LLM 前提の設計): グレースケール・長辺 2400px・JPEG q0.75・OCR オン(ja+en)・**見開き分割オン**(2026-08-16〜。縦長画像は分割しない)。
  OCR は縮小前のフル解像度で実行してから縮小する
